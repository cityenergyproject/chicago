define([
  'jquery',
  'underscore',
  'backbone',
  'router', // Request router.js
], function($, _, Backbone, AppRouter){
  var initialize = function(){
    
  };

  return {
    initialize: initialize
  };
});